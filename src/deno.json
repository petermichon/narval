{
  "tasks": {
    "install": "deno run --allow-run sh-install.ts",
    "build": "deno run --allow-run sh-build.ts",
    "serve": "deno run --allow-run sh-serve.ts",
    "dev": "deno run --allow-run --watch sh-serve.ts"
  },
  "lint": {
    "rules": {
      "include": [],
      "exclude": ["no-unused-vars", "no-empty", "no-explicit-any"]
    }
  }
}
