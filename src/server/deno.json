{
  "tasks": {
    "serve": "cd server ; deno run --allow-net --allow-read index.ts",
    "dev": "cd server ; deno run --allow-net --allow-read --watch index.ts"
  },
  "lint": {
    "rules": {
      "include": [],
      "exclude": ["no-unused-vars", "no-empty", "no-explicit-any"]
    }
  }
}
